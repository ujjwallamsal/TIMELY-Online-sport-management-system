# Backend environment example (no secrets). Copy to .env for local use.

# --- Core ---
DEBUG=1
SECRET_KEY=changeme-in-dev

# --- Database (PostgreSQL) ---
DB_NAME=timely_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# --- Redis (optional; used for Channels if set) ---
# Example: redis://127.0.0.1:6379/0
REDIS_URL=

# --- Frontend origin (for CORS/links) ---
FRONTEND_ORIGIN=http://localhost:5173

# --- Email ---
DEFAULT_FROM_EMAIL=noreply@example.com

# --- Stripe (leave empty for dev) ---
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- JWT lifetimes (optional overrides) ---
# ACCESS_TOKEN_LIFETIME_MIN=60
# REFRESH_TOKEN_LIFETIME_DAYS=7
